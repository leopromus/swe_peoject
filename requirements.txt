Django==4.2.5         # Latest stable version of Django
djangorestframework==3.14.0  # For building APIs (if needed)
django-crispy-forms==2.0  # Simplify form rendering
crispy-bootstrap5==0.7  # Bootstrap 5 integration for crispy forms
Pillow==9.4.0         # For image handling (optional, but good to have)
psycopg2==2.9.6       # PostgreSQL database driver (or use 'sqlite3' for local development)
django-filter==23.2   # For filtering querysets in views
django-allauth==0.56.1  # For user authentication and social login (optional)
python-decouple==3.8  # Manage secrets and environment variables
gunicorn==20.1.0      # Production-grade WSGI server
whitenoise==6.5.0     # Static file management in production
django-environ==0.10.0  # Environment variable support
stripe==5.2.0         # Payment integration for lawyers (if Stripe is used)
